# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

interface Connection {
  id: ID!
}

type DirectConnection implements Connection {
  id: ID!
  zombie: Zombie!
  address: String!
}

input DirectConnectionsFilterInput {
  zombieId: ID
}

type MQTTConnection implements Connection {
  id: ID!
  zombies: [Zombie!]!
  address: String!
  username: String
  password: String
}

input MQTTConnectionAuthInput {
  username: String!
  password: String
}

input MQTTConnectionsFilterInput {
  address: String
}

type Mutation {
  createZombie(id: ID!): Zombie!
  deleteZombie(id: ID!): Boolean!
  addDirectConnection(address: String!, zombieId: ID!): DirectConnection!
  removeDirectConnection(id: ID!, zombieId: ID!): Boolean!
  createMQTTConnection(
    auth: MQTTConnectionAuthInput
    address: String!
  ): MQTTConnection!
  deleteMQTTConnection(id: ID!): Boolean!
  setMQTTConnection(id: ID, zombieId: ID!): MQTTConnection!
}

type Query {
  zombie(id: ID!): Zombie
  zombies: [Zombie!]!
  directConnection(id: ID!): DirectConnection!
  directConnections(filter: DirectConnectionsFilterInput): [DirectConnection!]!
  mqttConnection(id: ID!): MQTTConnection!
  mqttConnections(filter: MQTTConnectionsFilterInput): [MQTTConnection!]!
}

type Zombie {
  id: ID!
  directConnections: [DirectConnection!]!
  mqttConnection: MQTTConnection
}
